/*Problem Def: Design a system to manage different types of vehicles. All vehicles have some common properties but different types of vehicles have their own specific features.

Class Vehicle with attributes brand and model and method displayInfo()
Derived class car with additional attribute numberofDoors, override method displayInfo() to include additional attribute.
Derived class MotorCycle with additional attribute hasCarrier. override method displayInfo() to include additional attribute.
Display information of all types of vehicles*/

// Base class
class Vehicle {
    String brand;
    String model;

    public void displayInfo() {
        System.out.println("Brand: " + brand);
        System.out.println("Model: " + model);
    }
}

// Derived class 1
class Car extends Vehicle {
    int numberOfDoors;

    // Overriding the method
    @Override
    public void displayInfo() {
        super.displayInfo(); // Calls the parent's method
        System.out.println("Number of Doors: " + numberOfDoors);
    }
}

// Derived class 2
class Motorcycle extends Vehicle {
    boolean hasCarrier;

    // Overriding the method
    @Override
    public void displayInfo() {
        super.displayInfo(); // Calls the parent's method
        System.out.println("Has Carrier: " + hasCarrier);
    }
}

// Main class to test everything
public class VehicleTest {
    public static void main(String[] args) {

        // Create a Car object
        Car myCar = new Car();
        myCar.brand = "Toyota";
        myCar.model = "Camry";
        myCar.numberOfDoors = 4;

        // Create a Motorcycle object
        Motorcycle myBike = new Motorcycle();
        myBike.brand = "Honda";
        myBike.model = "CBR";
        myBike.hasCarrier = false;

        // Display info
        System.out.println("--- Car Information ---");
        myCar.displayInfo();

        System.out.println("\n--- Motorcycle Information ---");
        myBike.displayInfo();
    }
}
